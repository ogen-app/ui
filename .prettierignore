# Generated by the TanStack Router plugin — never hand-edited, so never
# reformatted either. See CLAUDE.md.
src/routeTree.gen.ts

# Build output and caches.
dist
coverage
.tanstack
node_modules

# Lockfile and vendored assets.
pnpm-lock.yaml
public

# Untracked per-worktree dev overlay — it tints the logo per dev port and is
# deliberately never committed.
vite.config.js

# Prose is hand-wrapped in this repo and the wrapping is deliberate.
*.md
